services:
  symfony:
    image: ghcr.io/symfonycorp/symfony-php:8.2
    command: ["symfony", "serve", "--no-tls", "--ansi", "--port=8000", "--allow-http"]
    working_dir: /srv/app
    depends_on:
      database:
        condition: service_healthy
    volumes:
      - ./:/srv/app:rw,cached
      - symfony_var:/srv/app/var:rw
    environment:
      APP_ENV: ${APP_ENV:-dev}
      DATABASE_URL: ${DATABASE_URL:-postgresql://app:!ChangeMe!@database:5432/app?serverVersion=16&charset=utf8}
  grafana:
    image: grafana/grafana:10.4.5
    depends_on:
      database:
        condition: service_healthy
    volumes:
      - grafana_data:/var/lib/grafana:rw
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      GF_PATHS_PROVISIONING: /etc/grafana/provisioning
###> doctrine/doctrine-bundle ###
  database:
    image: postgres:${POSTGRES_VERSION:-16}-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app}
      # You should definitely change the password in production
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
      POSTGRES_USER: ${POSTGRES_USER:-app}
    healthcheck:
      test: ["CMD", "pg_isready", "-d", "${POSTGRES_DB:-app}", "-U", "${POSTGRES_USER:-app}"]
      timeout: 5s
      retries: 5
      start_period: 60s
    volumes:
      - database_data:/var/lib/postgresql/data:rw
      # You may use a bind-mounted host directory instead, so that it is harder to accidentally remove the volume and lose all your data!
      # - ./docker/db/data:/var/lib/postgresql/data:rw
###< doctrine/doctrine-bundle ###

volumes:
###> doctrine/doctrine-bundle ###
  database_data:
###< doctrine/doctrine-bundle ###
  symfony_var:
  grafana_data:
